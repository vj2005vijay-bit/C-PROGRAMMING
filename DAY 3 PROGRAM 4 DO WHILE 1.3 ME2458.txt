#include <stdio.h>
int main() {
    int num, factorial = 1, i = 1;
    printf("Enter a number: ");
    scanf("%d", &num);
    do {
        factorial *= i;
        i++;
    } while (i <= num);
    printf("Factorial of %d is %d\n", num, factorial);
    return 0;
}